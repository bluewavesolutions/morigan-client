const initialState: any = {
    layout: []
}
  
export function layoutReducer(
    state = initialState,
    action: any
  ): any {
    switch (action.type) {
      default:
        return state
    }
}